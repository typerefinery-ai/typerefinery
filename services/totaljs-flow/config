name                        : Total.js Flow v10
//cookie                      : typerefinery
//cookie_secret               : typerefinery
allow_custom_titles         : true

flowstream_worker           : fork

// Disables: Worker/Fork
// flowstream_worker        : false

// Uses WorkerThread instead of ChildProcess/Fork
// flowstream_worker        : true

// Replaces WorkerThread for the ChildProcess/Fork
// flowstream_worker        : fork

// Enables sandbox for NPM dependencies (we recommend to use it for "worker" mode only)
// flowstream_sandbox          : true

// Max. memory size for forked processes (in MB)
// flowstream_memory           : 4069

// Flow directory
directory               (env)    : SERVICE_DATA_PATH

//
// Set list of components to use in flows, typerefinery is the default list of components with totaljs as custom.
// Typerefinery component have group @TypeRefinery making these appear at the top.
//
// Own list of components and the structure must be the same as https://cdn.totaljs.com/flowstream/components/db.json
//flowstream_components         : https://typerefinery-ai.github.io/flowcomponents/db.json
// main
components                    : https://typerefinery-ai.github.io/flowcomponents/db.json
// custom
components2                   : https://cdn.totaljs.com/flowstream/components/db.json
